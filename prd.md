# 토스증권 API 기반 주식 자동매매 시스템 PRD

## 1. 개요

- 목적: 토스증권 Open API와 Python/FastAPI를 이용한 개인용 주식 자동매매 시스템 개발
- 대상: 국내 및 미국 주식
- 운영 원칙: 가상자금 자동매매로 시작하고, 안정성과 성과를 검증한 뒤 제한된 실거래로 전환
- 공식 문서: <https://developers.tossinvest.com/docs>

이 시스템은 실제 시세 수집, 전략 실행, 리스크 검사, 주문 처리, 체결 추적 및 운영 API를 제공한다. 안전을 위해 전략과 주문 실행을 분리하고, 기본 모드는 항상 가상매매(`paper`)로 설정한다.

## 2. 목표와 비목표

### 목표

- 국내·미국 주식 시세와 계좌 정보를 안정적으로 수집한다.
- 동일한 전략을 가상매매와 실거래에서 재사용한다.
- 주문 생성·정정·취소와 부분 체결을 포함한 전체 상태를 추적한다.
- 중복 주문, 과도한 손실, 연결 장애 및 상태 불일치를 방지한다.
- 모든 신호, 주문, 체결 및 리스크 판단을 감사 가능한 형태로 기록한다.
- 사용자 승인 없이 가상매매가 실거래로 전환되지 않게 한다.

### 비목표

- 초고빈도매매
- 옵션·선물·채권·가상자산 거래
- 복수 증권사 연동
- 타 사용자 대상 투자 서비스
- 공식 API가 지원하지 않는 주문 방식의 우회 구현

## 3. 기술 스택

| 영역 | 기술 |
|---|---|
| 언어 | Python 3.12 이상 |
| API | FastAPI |
| 서버 | Uvicorn |
| 비동기 처리 | `asyncio` |
| 데이터 검증 | Pydantic |
| 외부 통신 | 비동기 HTTP 및 WebSocket 클라이언트 |
| 저장소 | 초기 SQLite, 운영 시 PostgreSQL 검토 |
| 테스트 | pytest 계열 |

의존성 버전은 lock file로 고정한다. 금액·가격·수량 계산에는 `float` 대신 `Decimal`을 사용하고, 모든 시각은 timezone-aware 형식으로 저장한다.

## 4. 시스템 구조

```text
관리 UI / Client
       |
       v
FastAPI 운영 API
       |
       v
 Trading Engine
       |
       +-- Market Data Service
       +-- Strategy Engine
       +-- Risk Manager
       +-- Broker Interface
              |-- Paper Broker
              +-- Toss Broker
       +-- Order State Manager
       +-- Database / Audit Log
```

### 설계 원칙

- 전략은 토스 API를 직접 호출하지 않고 주문 의도만 생성한다.
- Paper Broker와 Toss Broker는 동일한 인터페이스를 구현한다.
- FastAPI는 조회·제어 계층이며 핵심 거래 로직은 순수 Python 모듈로 분리한다.
- 토스 REST, WebSocket 및 DB I/O는 비동기로 처리한다.
- 시장 데이터와 전략 사이에는 크기가 제한된 큐를 둔다.
- 시세는 최신 상태를 우선할 수 있지만 주문 이벤트는 유실시키지 않는다.

### 프로세스 정책

MVP는 FastAPI와 거래 엔진을 하나의 프로세스에서 실행한다.

- Uvicorn은 `--workers 1`로 실행한다.
- FastAPI `lifespan`에서 연결과 거래 엔진을 시작·종료한다.
- `--reload` 환경에서는 실거래를 금지한다.
- 거래 루프는 FastAPI `BackgroundTasks`로 실행하지 않는다.
- 다중 worker는 엔진 중복 실행과 중복 주문을 유발할 수 있으므로 금지한다.

확장이 필요하면 FastAPI와 거래 엔진을 별도 프로세스로 분리하고, 분산 락으로 계좌별 거래 엔진을 하나만 허용한다.

## 5. 토스증권 연동 요구사항

### 인증과 계좌

- REST 서버: `https://openapi.tossinvest.com`
- WebSocket: `wss://openapi-ws.tossinvest.com/ws/v1`
- OAuth 2.0 Client Credentials로 토큰을 발급한다.
- 계좌 API 호출에는 `X-Tossinvest-Account` 헤더를 사용한다.
- 토큰은 중앙 관리하며 secret과 토큰을 코드·로그·Git에 노출하지 않는다.
- 토큰 갱신 시 기존 토큰이 무효화될 수 있으므로 여러 프로세스가 독립 갱신하지 않는다.

### 시장 데이터

- 현재가, 호가, 체결, 상·하한가를 조회한다.
- 1분봉과 일봉을 조회하고 다른 주기는 내부에서 집계한다.
- 종목 정보와 시장 캘린더를 캐시한다.
- 캔들은 전략 입력 전에 시간 오름차순으로 정규화한다.
- 실시간 체결과 호가는 REST polling보다 WebSocket을 우선한다.

### WebSocket

- 실시간 체결, 호가 및 본인 주문 이벤트를 구독한다.
- 계정당 연결 2개, 연결당 구독 100개, 선언 초당 5회 제한을 준수한다.
- 연결 종료 시 지수 백오프와 jitter로 재연결하고 구독을 다시 선언한다.
- 재연결 후 REST 주문 조회로 유실 구간의 상태를 동기화한다.
- 시세 스트림은 중간 프레임이 유실될 수 있다는 전제로 처리한다.

### 주문

- 지정가·시장가 매수/매도와 주문 정정·취소를 지원한다.
- `quantity`와 `orderAmount` 중 하나만 사용한다.
- 미국 주식 금액 주문, 소수점 주문 및 주문 가능 시간 규칙을 검증한다.
- 모든 주문에 고유한 `clientOrderId`를 부여한다.
- 네트워크 타임아웃 시 실패로 단정하지 않고 같은 멱등키와 주문 조회로 결과를 확인한다.
- 주문의 대기, 부분 체결, 체결, 취소, 거부 및 정정 상태를 추적한다.
- 알 수 없는 신규 주문 상태가 와도 시스템이 중단되지 않게 한다.

### 호출 제한과 오류

- Rate Limit 응답 헤더를 확인해 요청 속도를 조절한다.
- 429 응답에는 `Retry-After`, 지수 백오프 및 jitter를 적용한다.
- 400·422는 자동 재시도하지 않는다.
- 401은 토큰을 갱신한 뒤 제한적으로 재시도한다.
- 403은 신규 주문을 중지하고 운영자에게 알린다.
- 500·503은 제한된 횟수로 재시도한다.

## 6. 가상자금 자동매매

최초 운영 단계에서는 실제 계좌 자금을 사용하지 않는다. 실제 토스 시세는 수신하지만 주문은 Paper Broker에서만 처리한다.

### 가상 계좌 기능

- 설정 가능한 KRW·USD 초기자금
- 사용 가능 현금과 주문 예약 현금 분리
- 보유 수량과 매도 예약 수량 분리
- 주문 접수, 부분 체결, 체결, 취소 및 거부 처리
- 수수료, 세금, 슬리피지 및 호가 반영
- 실현손익, 평가손익, 총자산 및 최대 낙폭 계산
- 주문·현금·포지션 상태 영속화 및 재시작 복구
- 미래 가격을 미리 사용하는 look-ahead bias 방지
- 가상자금 초기화와 충전 이력 기록

paper 모드에서는 토스 주문 생성·정정·취소 API가 절대로 호출되지 않아야 한다. 화면과 로그에는 현재 거래 모드를 명확히 표시한다.

## 7. 실거래 전환 정책

```text
백테스트
  → 실시간 가상매매
  → 안정성·성과 검증
  → 최소 금액 실거래
  → 제한된 실거래
  → 승인에 따른 한도 확대
```

실거래 승격은 자동으로 수행하지 않는다. 다음 기준을 충족한 뒤 사용자가 명시적으로 승인해야 한다.

| 구분 | 검증 기준 |
|---|---|
| 운영 표본 | 최소 운영 거래일과 체결 건수 충족 |
| 안정성 | 중복 주문, 미복구 주문 및 비정상 상태 0건 |
| 복구 | REST·WebSocket 장애 후 자동 복구 성공 |
| 상태 일치 | 주문·현금·포지션 동기화 성공 |
| 리스크 | 최대 낙폭과 일일 손실이 설정 한도 이내 |
| 비용 | 수수료·세금·보수적 슬리피지 반영 |
| 성과 | 비용 차감 후 전략별 성과 기준 충족 |
| 운영 준비 | 알림, 킬 스위치, 백업 및 감사 로그 검증 |

### 전환 안전장치

- live 모드는 별도 설정, 실계좌 자격 증명, 사용자 승인 및 리스크 한도를 요구한다.
- paper와 live의 DB 상태 및 주문 ID 영역을 분리한다.
- 가상 포지션을 실제 보유 포지션으로 이전하지 않는다.
- 최초 실거래는 제한된 종목과 최소 주문금액으로 시작한다.
- 주문·손실·총투자 한도는 검토와 승인 없이 확대하지 않는다.
- 기준 위반 시 신규 주문을 차단하고 필요하면 paper 모드로 복귀한다.

## 8. 리스크 관리

모든 주문은 Broker로 전달되기 전에 다음 검사를 통과해야 한다.

- 매수 가능 금액 또는 매도 가능 수량
- 1회 최대 주문금액
- 종목별 최대 보유금액 또는 비중
- 일일 최대 손실
- 미체결 주문 수
- 장 운영시간과 휴장일
- 상·하한가, 거래정지 및 매수 유의사항
- 중복 신호와 중복 주문

킬 스위치는 신규 주문을 즉시 차단한다. 기존 미체결 주문을 함께 취소할지는 별도 정책으로 설정한다.

## 9. FastAPI 운영 API

| Method | Path | 용도 |
|---|---|---|
| `GET` | `/health/live` | 프로세스 생존 확인 |
| `GET` | `/health/ready` | DB·연결·엔진 준비 상태 |
| `GET` | `/api/v1/system/status` | 모드, 연결 및 엔진 상태 |
| `POST` | `/api/v1/engine/start` | 엔진 시작 |
| `POST` | `/api/v1/engine/stop` | 안전한 엔진 중지 |
| `POST` | `/api/v1/risk/kill-switch` | 신규 주문 차단 |
| `GET` | `/api/v1/paper/account` | 가상 현금·포지션·손익 |
| `POST` | `/api/v1/paper/reset` | 가상 계좌 초기화 |
| `GET` | `/api/v1/positions` | 포지션 조회 |
| `GET` | `/api/v1/orders` | 주문 목록 조회 |
| `POST` | `/api/v1/orders/{id}/cancel` | 주문 취소 |
| `GET` | `/api/v1/strategies` | 전략과 실행 상태 조회 |
| `POST` | `/api/v1/strategies/{id}/enable` | 전략 활성화 |
| `POST` | `/api/v1/strategies/{id}/disable` | 전략 비활성화 |
| `GET` | `/api/v1/promotion/report` | 실거래 승격 검증 결과 |
| `WS` | `/ws/v1/events` | 관리 화면용 실시간 상태 |

상태 변경 API에는 인증과 감사 로그가 필요하다. 외부 공개 시 TLS, 강한 인증, 접근 제어, CORS 제한 및 요청 제한을 적용한다. 초기에는 localhost 또는 사설망 접근을 우선한다.

## 10. 저장 및 관측

### 저장 데이터

- 전략 신호와 주문 의도
- 리스크 판단 결과
- API 요청 결과와 오류 코드
- 주문, 체결, 현금 및 포지션 변경
- `clientOrderId`, 토스 `orderId`, 전략 ID 연결 정보
- 실행 모드 변경과 관리자 작업

### 운영 지표

- REST 성공률, 지연시간 및 남은 호출 한도
- WebSocket 연결 상태와 재연결 횟수
- 마지막 시장 데이터 수신 시각
- 주문 접수·체결·거부 건수와 처리 지연
- 현재 포지션, 일일 손익 및 리스크 한도 사용률
- 주문·현금·포지션 동기화 결과

시스템 재시작 시 DB 상태와 토스 계좌의 미체결 주문·보유 자산을 먼저 동기화한 후 신규 주문을 허용한다.

## 11. 주요 테스트

- 토큰 만료와 동시 갱신
- 429 및 백오프
- 주문 직후 네트워크 타임아웃과 멱등 재시도
- 부분 체결 후 취소·거부
- WebSocket 단절, 재연결 및 REST 동기화
- 휴장일, 시장 세션 및 미국 소수점 주문시간
- 일일 손실 한도와 킬 스위치
- 가상 계좌의 현금·수량 예약과 재시작 복구
- 수수료·세금·슬리피지를 포함한 가상 체결
- paper 모드에서 토스 주문 API 미호출
- 승인 없는 live 전환 차단
- FastAPI `lifespan`과 graceful shutdown
- 다중 거래 엔진 실행 방지

## 12. 구현 단계

### Phase 1 — 기반

FastAPI, 설정, DB, 토큰 관리자, 토스 REST 클라이언트, health API를 구현한다.

### Phase 2 — 가상매매

시장 데이터, 전략 인터페이스, Paper Broker, 가상 계좌 원장, 리스크 관리 및 성과 보고서를 구현한다.

### Phase 3 — 실시간 안정화

WebSocket, 주문 상태 머신, 재연결, 상태 동기화, 모니터링 및 장애 복구를 구현한다.

### Phase 4 — 제한된 실거래

Toss Broker, 주문 생성·정정·취소, 승격 보고서, 수동 승인 및 최소 금액 실거래 모드를 구현한다.

### Phase 5 — 운영

장시간 안정성 시험을 통과한 뒤 승인에 따라 실거래 한도를 단계적으로 확대한다.

## 13. 완료 기준

- 가상매매가 실제 주문 API를 호출하지 않고 연속 실행된다.
- 재시작 후 주문·현금·포지션이 복원된다.
- 연결 장애 후 주문과 계좌 상태가 자동 동기화된다.
- 동일 주문이 중복 생성되지 않는다.
- 설정된 리스크 한도를 넘는 주문이 차단된다.
- 사용자 승인 없이는 live 모드를 시작할 수 없다.
- 최소 금액 실거래에서 주문 전체 과정을 추적할 수 있다.

## 14. 미결정 사항

- 최초 매매 전략과 진입·청산 규칙
- 가상 초기자금과 기준 통화
- 실거래 승격에 필요한 거래일·체결 수·성과 기준
- 최초 출시 시장과 허용 종목
- 운영 DB와 배포 환경
- 알림 채널
- 킬 스위치 실행 시 미체결 주문 자동 취소 여부

## 15. 참고 문서

- 토스증권 Open API: <https://developers.tossinvest.com/docs>
- OpenAPI: <https://openapi.tossinvest.com/openapi-docs/latest/openapi.json>
- AsyncAPI: <https://openapi.tossinvest.com/openapi-docs/latest/asyncapi.json>
- FastAPI lifespan: <https://fastapi.tiangolo.com/advanced/events/>
- FastAPI WebSocket: <https://fastapi.tiangolo.com/advanced/websockets/>
- FastAPI 배포: <https://fastapi.tiangolo.com/deployment/server-workers/>
